from database import user
from database import record
from database import journal
#print(record.get_record_by_account('jl'))
# key_list=record.get_key_by_account('jl')
# info_len=record.get_info_length('jl')
# borrow_list=[]
# borrow={
#     'name':"",
#     'status':"",
#     'time_1':"",
#     'time_2':""
# }
# for i in range(info_len):
#     borrow['name']=journal.get_name_by_key(key_list[i])
#     borrow['status']=